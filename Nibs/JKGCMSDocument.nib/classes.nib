{
    IBClasses = (
        {CLASS = DeleteTableView; LANGUAGE = ObjC; SUPERCLASS = NSTableView; }, 
        {
            ACTIONS = {removeSpectrum = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = JKMainDocument; LANGUAGE = ObjC; SUPERCLASS = NSDocument; }, 
        {
            ACTIONS = {
                abort = id; 
                confirm = id; 
                discard = id; 
                editLibrary = id; 
                fitChromatogramDataToView = id; 
                fitSpectrumDataToView = id; 
                identifyPeaks = id; 
                next = id; 
                obtainBaseline = id; 
                other = id; 
                previous = id; 
                renumberPeaks = id; 
                showMassChromatogram = id; 
            }; 
            CLASS = JKMainWindowController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                baselineController = NSArrayController; 
                chromatogramDataSeriesController = NSArrayController; 
                chromatogramView = MyGraphView; 
                detailsSplitSubview = RBSplitSubview; 
                detailsTabView = NSTabView; 
                detailsTabViewItemView = NSView; 
                mainWindowSplitView = NSView; 
                moleculeSplitSubview = RBSplitSubview; 
                moleculeView = JKMoleculeView; 
                peakController = NSArrayController; 
                peaksTable = NSTableView; 
                printAccessoryView = NSView; 
                progressBar = NSProgressIndicator; 
                progressSheet = NSWindow; 
                resultsTable = NSTableView; 
                searchResultsController = NSArrayController; 
                searchResultsTabViewItemView = NSView; 
                spectrumDataSeriesController = NSArrayController; 
                spectrumView = MyGraphView; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = JKMoleculeView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {CLASS = JKPathPopUpButton; LANGUAGE = ObjC; SUPERCLASS = NSPopUpButton; }, 
        {
            ACTIONS = {centerOrigin = id; lowerLeftOrigin = id; showAll = id; squareAxes = id; }; 
            CLASS = MyGraphView; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSView; 
        }, 
        {CLASS = NSObject; LANGUAGE = ObjC; }
    ); 
    IBVersion = 1; 
}